# VULNERA — all Python dependencies (pipeline, web backend, optional LLM)
#
# GPU: install PyTorch for your CUDA build first, then install the rest:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements.txt
#
# Keep transformers <5 — v5.x needs newer PyTorch dtypes than 2.6 provides.

# PyTorch stack (encoder, embedder, scan runtime, LLM)
torch>=2.5,<2.7
transformers>=4.46,<5
tokenizers>=0.21,<0.22
accelerate>=1.0
safetensors>=0.4.0
huggingface-hub>=0.24.0

# Data processing & parquet
numpy>=1.24
pandas>=2.0
pyarrow>=14.0
pyyaml>=6.0

# Tree ensemble, meta learner, calibration
scikit-learn>=1.3
joblib>=1.3
xgboost>=2.0
lightgbm>=4.0

# Web API & scan runtime
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
pydantic>=2.0
psutil>=5.9
reportlab>=4.0
tqdm>=4.66

# Explainability (SHAP token attribution)
shap>=0.44.0

# Local LLM explanations (4-bit Qwen; Linux/WSL + CUDA recommended)
bitsandbytes>=0.44.0

# Dataset download (Google Drive metadata for DiverseVul)
gdown>=5.0
